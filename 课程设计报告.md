# 《网络编程与应用》课程设计报告

## DevRadar —— GitHub 热门项目与开发者动态追踪器

---

| 项目 | 内容 |
|------|------|
| 课程名称 | 网络编程与应用 |
| 项目名称 | DevRadar |
| 技术栈 | Python + PyQt6 + TCP Socket + SQLite + GitHub REST API + BeautifulSoup4 + matplotlib |
| 开发环境 | Windows 11, Python 3.10+, Miniconda |

---

## 目录

1. [项目概述](#1-项目概述)
2. [需求分析](#2-需求分析)
3. [系统设计](#3-系统设计)
4. [详细实现](#4-详细实现)
5. [测试报告](#5-测试报告)
6. [总结与展望](#6-总结与展望)
7. [参考文献](#7-参考文献)

---

## 1. 项目概述

### 1.1 项目背景

GitHub 作为全球最大的代码托管平台，每天产生海量的开源项目和开发者活动。然而，GitHub 站内搜索功能有限，难以高效发现高质量项目；同时，开发者关注的大神和仓库的动态分散在多个页面，缺乏统一的实时追踪工具。

### 1.2 项目目标

开发一个名为 **DevRadar** 的桌面应用程序，实现以下核心目标：

- **高效发现**：通过智能搜索和多维筛选，快速定位高质量 GitHub 项目
- **实时追踪**：通过 Socket 实时推送关注目标的动态
- **知识沉淀**：自动生成个人技术简报，帮助用户沉淀每周/每月的技术收获
- **深度洞察**：通过 Star 增长曲线和项目健康度指标，判断项目的投入价值

### 1.3 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| GUI 框架 | PyQt6 | 专业控件、QSystemTrayIcon、信号/槽机制 |
| Socket 通信 | Python 标准库 socket (TCP) | 轻量、满足本地进程通信需求 |
| HTTP 客户端 | requests | 灵活、支持会话和重试机制 |
| 爬虫解析 | BeautifulSoup4 | 成熟的 HTML 解析库 |
| 数据库 | SQLite (sqlite3) | 零配置、嵌入式、适合桌面应用 |
| 图表 | matplotlib | 数据可视化、可嵌入 PyQt 窗口 |
| 并发 | threading + queue.Queue | 网络请求异步化、不阻塞 GUI |

---

## 2. 需求分析

### 2.1 功能需求

项目按照 6 大核心功能模块组织：

| 模块 | 功能描述 | 用户价值 |
|------|----------|----------|
| 模块 1 | 全局智能搜索与发现引擎 | 解决 GitHub 搜索低效问题 |
| 模块 2 | 趋势榜单透视 | 随时了解技术潮流 |
| 模块 3 | 定点监控与智能过滤 | 只关注重要事件，减少噪音 |
| 模块 4 | 实时动态推送与桌面通知 | 实时获取更新，不接受延迟 |
| 模块 5 | 个人技术简报生成器 | 自动生成专属技术报告 |
| 模块 6 | 项目洞察面板 | 判断项目是否值得投入 |

### 2.2 非功能需求

- **响应性**：所有网络操作在子线程执行，GUI 永不卡顿
- **容错性**：API 限流感知、爬虫失效自动切换备用方案、Socket 断线自动重连
- **持久化**：监控列表、事件历史、收藏夹、搜索历史均持久化到 SQLite
- **可扩展性**：数据获取、通信、界面三层严格分离

---

## 3. 系统设计

### 3.1 系统架构

系统采用 **客户端-本地服务端** 架构，本地服务端作为单实例后台进程，GUI 客户端通过 TCP Socket 与之通信。

```
┌─────────────────────────────────────────────────────────────┐
│                     DevRadar 整体架构                        │
└─────────────────────────────────────────────────────────────┘

[GitHub REST API]     [GitHub Trending HTML]
       ▲                      ▲
       │     HTTP 请求         │
       │                      │
┌──────┴──────────────────────┴───────────────────────────────┐
│                    服务端进程                                 │
│  ┌────────────┐  ┌───────────┐  ┌────────────────────────┐  │
│  │ github_api │  │  trending │  │  monitor_scheduler     │  │
│  │ API 客户端  │  │ 爬虫+备用  │  │  轮询调度器+过滤引擎   │  │
│  └─────┬──────┘  └─────┬─────┘  └───────────┬────────────┘  │
│        │               │                     │               │
│        ▼               ▼                     ▼               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                    server.py                            │  │
│  │          TCP Socket 服务器 (127.0.0.1:9876)              │  │
│  │          命令分发 / 消息推送 / 心跳处理                   │  │
│  └──────────────────────┬─────────────────────────────────┘  │
│                         │                                     │
│  ┌──────────────────────┴─────────────────────────────────┐  │
│  │                  db_manager.py                          │  │
│  │       SQLite (6 表: monitors/events/trending_cache/    │  │
│  │        star_snapshots/bookmarks/search_history)        │  │
│  └────────────────────────────────────────────────────────┘  │
└───────────────────────────┬──────────────────────────────────┘
                            │
                     TCP Socket (JSON 消息, \\n 分隔)
                            │
┌───────────────────────────▼──────────────────────────────────┐
│                    GUI 客户端                                 │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                  main_window.py                         │  │
│  │     主窗口 / 深色主题 / 系统托盘 / 菜单栏 / 状态栏      │  │
│  └──┬───────┬───────┬────────┬─────────┬─────────────────┘  │
│     │       │       │        │         │                     │
│     ▼       ▼       ▼        ▼         ▼                     │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌───────┐ ┌───────────────┐    │
│  │search│ │trend │ │monit │ │stream │ │insight_dialog │    │
│  │_panel│ │_panel│ │_panel│ │_panel │ │ 项目洞察+图表 │    │
│  │ 搜索  │ │ 趋势 │ │ 监控 │ │ 消息流 │ │               │    │
│  └──────┘ └──────┘ └──────┘ └───────┘ └───────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                  client.py                              │  │
│  │       Socket 客户端 / 自动重连 / 心跳 / 信号分发        │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 通信协议设计

所有消息为 **JSON 对象**，以 `\n` 结尾。

**消息类型定义：**

```
search_result  — 搜索结果
trending_data  — 趋势数据
new_event      — 新事件推送
command        — 客户端指令
ack            — 服务端应答
status         — 状态信息
heartbeat      — 心跳
report_result  — 简报生成结果
```

**通信流程示例：**

```
客户端                         服务端
  │                             │
  │  {"type":"command",         │
  │   "action":"search_repos",  │
  │   "query":"flask"}          │
  │ ──────────────────────────> │
  │                             │ —— 调用 GitHub Search API
  │                             │
  │  {"type":"search_result",   │
  │   "data":[...],             │
  │   "rate_remaining":4990}    │
  │ <────────────────────────── │
  │                             │
  │  {"type":"heartbeat"}       │ （每 30 秒）
  │ ──────────────────────────> │
  │  {"type":"heartbeat"}       │
  │ <────────────────────────── │
```

### 3.3 数据库设计

使用 SQLite，数据库文件 `data/devradar.db`，包含 6 张表：

**monitors（监控目标）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| target | TEXT UNIQUE | 目标（如 "torvalds" 或 "torvalds/linux"） |
| type | TEXT | 'user' 或 'repo' |
| filters | TEXT (JSON) | 过滤规则 |
| added_at | TIMESTAMP | 添加时间 |

**events（事件记录）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| monitor_id | INTEGER FK | 关联 monitors |
| event_type | TEXT | GitHub 事件类型 |
| payload | TEXT (JSON) | 完整事件数据 |
| repo_name | TEXT | 仓库名 |
| actor | TEXT | 操作者 |
| url | TEXT | 事件链接 |
| received_at | TIMESTAMP | 接收时间 |

**trending_cache（趋势缓存）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| language | TEXT | 编程语言 |
| since | TEXT | 'daily' / 'weekly' / 'monthly' |
| repo_id | TEXT | 仓库标识 |
| repo_full_name | TEXT | 仓库全名 |
| data | TEXT (JSON) | 完整数据 |
| fetched_at | TIMESTAMP | 抓取时间 |

**star_snapshots（Star 快照）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| repo_full_name | TEXT | 仓库全名 |
| star_count | INTEGER | Star 数量 |
| recorded_at | TIMESTAMP | 记录时间 |

**bookmarks（收藏夹）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| repo_full_name | TEXT UNIQUE | 仓库全名 |
| repo_url | TEXT | 仓库链接 |
| description | TEXT | 描述 |
| language | TEXT | 语言 |
| stars | INTEGER | Star 数 |
| saved_at | TIMESTAMP | 收藏时间 |

**search_history（搜索历史）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| query | TEXT | 搜索词 |
| searched_at | TIMESTAMP | 搜索时间 |

---

## 4. 详细实现

### 4.1 项目结构

```
DevRadar/
├── run.py                          # 一键启动 (服务端+GUI)
├── requirements.txt                # 依赖清单
├── README.md                       # 项目文档
│
├── src/
│   ├── common/                     # 公共模块
│   │   ├── config.py               # 配置管理 (环境变量 > JSON > 默认值)
│   │   ├── logger.py               # 统一日志 (文件轮转+控制台)
│   │   └── protocol.py             # 通信协议 (消息类型/编解码)
│   │
│   ├── server/                     # 服务端
│   │   ├── github_api.py           # GitHub REST API 封装 (限流/重试/缓存)
│   │   ├── trending.py             # Trending 爬虫 (HTML→API 自动切换)
│   │   ├── db_manager.py           # SQLite 数据持久层 (线程安全)
│   │   ├── server.py               # TCP Socket 服务器 (14 个命令处理器)
│   │   ├── monitor_scheduler.py    # 监控轮询调度器 (过滤规则引擎)
│   │   ├── snapshot_manager.py     # Star 快照采集器 (每 6 小时)
│   │   └── report_generator.py     # 技术简报/洞察数据生成器
│   │
│   └── client/                     # GUI 客户端
│       ├── client.py               # Socket 客户端 (自动重连+心跳)
│       ├── main_window.py          # 主窗口 (深色主题+系统托盘)
│       └── panels/
│           ├── search_panel.py     # 模块1: 全局搜索面板
│           ├── trending_panel.py   # 模块2: 趋势榜单面板
│           ├── monitor_panel.py    # 模块3: 监控管理面板
│           ├── stream_panel.py     # 模块4: 实时动态流面板
│           └── insight_dialog.py   # 模块6: 项目洞察弹窗
│
└── data/                           # 运行时数据
    ├── devradar.db                 # SQLite 数据库
    ├── devradar.log                # 日志文件
    └── config.json                 # 用户配置文件
```

### 4.2 各模块关键技术点

#### 模块 1：全局搜索 (search_panel.py + github_api.py)

- 调用 `GET /search/repositories`，使用 `in:name` 参数优先匹配项目名
- 支持多维筛选：编程语言、Star 范围（`stars:>=N` / `stars:N..M`）
- 搜索结果通过 `queue.Queue` 发送给服务端，服务端处理完成后通过 Socket 返回
- 搜索历史自动保存到 `search_history` 表，支持清空
- API 配额可视化：状态栏显示剩余次数，< 50 橙色、< 10 红色

#### 模块 2：趋势榜单 (trending_panel.py + trending.py)

- **双模式爬虫**：主方案解析 GitHub Trending HTML (`article.Box-row`)，备用方案调用第三方 API
- 失败自动切换：HTML 解析结果为空时自动调用备用 API，记录日志
- 缓存机制：结果存入 `trending_cache` 表，减少重复请求
- 自动刷新：QTimer 定时触发刷新（默认 10 分钟）
- 语言标签配色：为每种编程语言定义圆角背景色

#### 模块 3：定点监控 (monitor_panel.py + monitor_scheduler.py)

- 监控目标验证：添加时调用 GitHub API 验证目标存在性，404 即时拒绝
- 过滤规则引擎：支持事件类型过滤 + 关键词过滤
- 轮询调度：独立线程定时轮询（默认 5 分钟），应用过滤规则后推送
- 事件摘要生成：为不同事件类型（Push/Release/Issue/PR）生成可读摘要

#### 模块 4：实时推送 (stream_panel.py + client.py)

- **断线自动重连**：指数退避（1s → 30s），永久循环直至连接恢复
- 心跳检测：客户端每 30 秒发送心跳，服务端即时响应
- 3 秒高亮动画：新事件插入时设置金色/青色背景，3 秒后自动恢复
- 系统通知：QSystemTrayIcon 弹出桌面 toast 通知

#### 模块 5：技术简报 (report_generator.py)

- 数据全部来自本地数据库，零额外 API 调用
- 内容组织：关注动态（按 Release/Push/Issue/PR 分组）→ 趋势 Top 5 → 收藏夹 → 搜索历史
- 输出格式：标准 Markdown，可预览、复制、保存
- 空数据友好提示：无事件时显示幽默占位文案

#### 模块 6：项目洞察 (insight_dialog.py + report_generator.py)

- matplotlib 折线图嵌入 PyQt 窗口，深色主题配色
- Star 历史曲线：基于 `star_snapshots` 表数据绘制
- 数据积累提示：不足 7 天显示 "数据收集中 (X/7 天)"
- 项目健康度：最近提交时间、活跃贡献者、开源协议、话题标签

### 4.3 错误处理策略

| 场景 | 处理方式 | 代码位置 |
|------|----------|----------|
| API 限流 | 检查 X-RateLimit-Remaining，< 20 暂停轮询 | `github_api.py` |
| 网络超时 | timeout=15s，指数退避重试 3 次 | `github_api.py` |
| Token 无效 | 返回 401 时弹出错误提示 | `github_api.py` |
| 爬虫失效 | HTML 解析为空 → 自动切 API 备用 | `trending.py` |
| Socket 断线 | 指数退避重连 + 界面状态更新 | `client.py` |
| 数据库错误 | try-except 包裹，return 友好提示 | `db_manager.py` |
| UI 超时 | 15 秒 QTimer 保护，自动恢复按钮状态 | `search/trending panel` |
| JSON 序列化 | 自定义编码器处理 datetime 等非标准类型 | `protocol.py` |

---

## 5. 测试报告

### 5.1 集成测试

端到端集成测试验证了所有核心功能的 Socket 通信：（13 项全部通过）

| 测试项 | 预期 | 结果 |
|--------|------|------|
| 服务端启动 | 端口成功监听 | ✅ |
| TCP 连接 | 客户端连接成功 | ✅ |
| 搜索仓库 | 返回搜索结果 | ✅ |
| 获取趋势 | 返回 Trending 数据 | ✅ |
| 添加监控 (torvalds) | 提示添加成功 | ✅ |
| 添加监控 (torvalds/linux) | 提示添加成功 | ✅ |
| 监控列表 | 返回所有监控 | ✅ |
| 心跳检测 | 服务端响应心跳 | ✅ |
| 项目洞察 | 返回健康度数据 | ✅ |
| 生成简报 | 返回 Markdown 内容 | ✅ |
| 收藏功能 | 添加 + 列出收藏 | ✅ |
| 限流查询 | 返回剩余配额 | ✅ |

### 5.2 API 连通性测试

```
API rate limit: 4997/5000 ✓
flask repo: 71754 stars ✓
Trending: 16 repos fetched ✓
User torvalds: 30 events ✓
Repo torvalds/linux: 237665 stars ✓
```

### 5.3 语法检查

全部 23 个 Python 源文件通过语法编译检查，零错误。

---

## 6. 总结与展望

### 6.1 项目总结

DevRadar 项目成功实现了一个功能完整的 GitHub 项目追踪桌面应用，涵盖搜索、趋势、监控、推送、简报、洞察六大核心功能。项目采用客户端-服务端架构，通过 TCP Socket 实现实时通信，三层分离的设计为未来扩展提供了良好基础。

**技术亮点：**

1. **双模容错爬虫**：Trending 数据同时支持 HTML 解析和 API 备用方案，页面改版不影响功能
2. **限流感知架构**：全局监控 API 配额，低配额时自动降级
3. **断线自动重连**：Socket 断线后指数退避重连，无需用户干预
4. **深色主题 UX**：专业级暗色界面，状态栏实时显示连接和 API 状态
5. **零配置启动**：`python run.py` 一键启动，服务端和 GUI 自动联动

### 6.2 改进方向

1. **定时简报推送**：实现定时自动生成简报（如每周一 9:00）
2. **Star 预警机制**：单日 Star 增长超过阈值时触发特别通知
3. **多语言支持**：支持更多编程语言的 Trending 数据
4. **代理支持**：添加 HTTP/HTTPS 代理配置，适应受限网络环境
5. **数据导出**：支持导出为 JSON/CSV 格式
6. **打包分发**：使用 PyInstaller 打包为独立 exe

---

## 7. 参考文献

1. GitHub REST API Documentation. https://docs.github.com/en/rest
2. PyQt6 Documentation. https://www.riverbankcomputing.com/static/Docs/PyQt6/
3. Python socket — Low-level networking interface. https://docs.python.org/3/library/socket.html
4. Beautiful Soup Documentation. https://www.crummy.com/software/BeautifulSoup/bs4/doc/
5. Matplotlib Documentation. https://matplotlib.org/stable/contents.html
6. SQLite Documentation. https://www.sqlite.org/docs.html
7. requests: HTTP for Humans. https://docs.python-requests.org/
